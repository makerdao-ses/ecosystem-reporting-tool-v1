import React from 'react';
import { Container } from "theme-ui"

export default function NotAuthenticated() {
    return <>
        <Container>
            <p>Authenticate in settings</p>
        </Container>
    </>
}